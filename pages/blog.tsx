import React, { FC } from "react";
import DefaultLayout from "@/layouts/DefaultLayout";

const BlogPage: FC = () => {
  return <DefaultLayout title="Blogs">Blog page</DefaultLayout>;
};

export default BlogPage;
